<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".FoundryApplication"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeFoundry">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 独立渲染进程：多进程沙箱隔离 SDK 执行，避免 ClassLoader 冲突与 OOM 连锁 -->
        <service
            android:name=".renderer.RendererService"
            android:process=":renderer"
            android:exported="false" />

        <!-- 隐形截屏 Activity：Service 无 Window → 透明 Activity 渲染 → drawToBitmap → 文件保存 → 自毁 -->
        <activity
            android:name=".renderer.RenderCaptureActivity"
            android:process=":renderer"
            android:theme="@style/Theme.Transparent.NoAnimation"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:showWhenLocked="false"
            android:turnScreenOn="false" />

    </application>

</manifest>
